# Mobile app (Expo)

Scaffold only. Next steps:

- `npx create-expo-app .` (inside this folder) OR we keep using the minimal skeleton and add deps manually.
- The app stores an **API base URL** (ngrok URL) and calls:
  - `GET /health`
  - `POST /chat`

This repo intentionally does not bootstrap the full Expo project yet (per your request).

