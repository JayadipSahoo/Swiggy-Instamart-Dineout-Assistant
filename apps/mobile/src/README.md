# Mobile source (planned)

When we generate the Expo app, we’ll place the code here:

- `src/screens/ChatScreen.tsx`
- `src/components/*`
- `src/lib/api.ts`
- `src/lib/storage.ts`

For now this is a placeholder folder so the structure is ready.

